# Architecture
Python utility library (pip package). Public API lives in `src/mylib/api.py`.
Internal helpers live in `src/mylib/_internal/`. Prefix `_` denotes private, not exported.
Package version: 0.4.1. No external consumers of internal modules.

