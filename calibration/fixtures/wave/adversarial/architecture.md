# Architecture
React + TypeScript SPA. Global theme defined in /src/styles/theme.css.
App.tsx wraps all routes. Shared layout components (NavBar, Footer, PageWrapper) used on every page.
design tokens: --color-primary, --color-text, --color-bg-* series.
