# Architecture
Full-stack monorepo: React frontend (/frontend), FastAPI backend (/backend), Postgres (/migrations).
Single developer, early-stage product. No external consumers. One environment: production.
