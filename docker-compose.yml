services:
  core:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: commit-matrix-core:latest
    container_name: commit-matrix-core
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - HOST_REPO_OWNER=${HOST_REPO_OWNER:-local}
      - MOCK_SCORE=${MOCK_SCORE:-false}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./.git:/app/.git:ro
      - ./rubrics:/app/rubrics
      - ./backend:/app/backend
      - ./templates:/app/templates
      - ./static:/app/static
      - ./VERSION:/app/VERSION
      - /var/run/docker.sock:/var/run/docker.sock
